#include "ProductInfo.h"

namespace padflow::product {
static_assert(!name.empty());
static_assert(!company.empty());
static_assert(!bundleId.empty());
static_assert(schemaVersion == 1);
} // namespace padflow::product
