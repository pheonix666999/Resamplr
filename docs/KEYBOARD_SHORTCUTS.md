# Keyboard Shortcuts

Product shortcuts are deferred until their commands exist. Milestone 0 exposes no sampler shortcuts.
The final mapping will use command IDs, accessible menu labels, focus-safe behavior, configurable pad
keys, and documentation generated or checked against the command registry.

